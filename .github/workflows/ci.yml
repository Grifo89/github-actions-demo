name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:  # Indented under 'build'
      - name: Checkout code
        uses: actions/checkout@v4

      - name: List files
        run: ls -la   